import { PriceAfterOfferPipe } from './price-after-offer.pipe';

describe('PriceAfterOfferPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceAfterOfferPipe();
    expect(pipe).toBeTruthy();
  });
});
